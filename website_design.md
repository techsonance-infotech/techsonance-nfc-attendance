<user_request>Create a mobile app for me where a employee enter the office it should check the location of the office and when it gets the location it should add a time in of entering the office and they should time out manualy if they don't enter the time out it should be added to leave</user_request>

<todo_list>
1. Create employee attendance tracking system with geolocation-based auto check-in that verifies office location coordinates and logs time-in automatically
2. Build manual time-out interface with confirmation dialog and implement auto-leave marking logic for missing time-outs
3. Create attendance history page showing daily records with time-in, time-out, duration, and leave status
4. Add settings page for configuring office location coordinates and geofence radius
5. Design mobile-responsive UI with status indicators, current session display, and attendance summary on homepage
</todo_list>